(()=>{
'use strict';
const U=window.UGV21;if(!U)return;
const {PLAYERS,$,$$,fmt,loadClog,TKEY}=U;
function valid(x){if(!x||typeof x!=='object'||x.error||x.errors)return false;const d=x.data??x;return !!(d&&typeof d==='object'&&d.items&&typeof d.items==='object')}
function ownedIds(log){if(!valid(log))return null;const d=log.data??log,root=d.items||{},ids=new Set();function walk(n){if(Array.isArray(n)){n.forEach(walk);return}if(!n||typeof n!=='object')return;if(Number.isFinite(+n.id)){if(Math.max(0,+(n.count??n.quantity??0))>0)ids.add(+n.id);return}Object.values(n).forEach(walk)}walk(root);return ids}
async function renderOverviewClog(){if(document.body.dataset.page!=='home')return;let doc;try{doc=await loadClog()}catch{return}const owned=new Map(),owners=new Map();for(const p of PLAYERS.filter(p=>U.loadMemberSelection().has(p.key))){const ids=ownedIds(doc?.players?.[p.key]);owned.set(p.key,ids);if(ids)for(const id of ids){if(!owners.has(id))owners.set(id,new Set());owners.get(id).add(p.key)}}for(const card of $$('.player-card,.side-card')){const name=$('h3',card)?.textContent?.trim(),p=PLAYERS.find(x=>x.name===name);if(!p)continue;$('.v22-clog-quick',card)?.remove();const ids=owned.get(p.key),box=document.createElement('div');box.className='v22-clog-quick';if(!ids){box.innerHTML='<div><span>Collection Log</span><b>Unknown</b><small>no data available yet</small></div>'}else{let exclusive=0,shared=0;for(const id of ids)(owners.get(id)?.size===1?exclusive++:shared++);box.innerHTML=`<div><span>Unlocked</span><b>${fmt(ids.size)}</b><small>Collection Log slots</small></div><div><span>Exclusive in selection</span><b>${fmt(exclusive)}</b><small>only this member has them</small></div><div><span>Shared</span><b>${fmt(shared)}</b><small>also held by another member</small></div>`}card.append(box)}}
function removeOwnerControls(){$$('[data-shared-refresh]').forEach(x=>x.remove());for(const f of $$('.footer')){if($('.v22-dev-link',f))continue;const a=document.createElement('a');a.href='developer.html';a.className='v22-dev-link';a.textContent='Developer options';a.setAttribute('aria-label','Developer options and shared-data maintenance');f.append(a)}}
function simplifyMemberFilters(root=document){$$('.v21-member-actions',root).forEach(x=>x.remove())}
function hardenSavedClogReload(){for(const b of $$('[data-reload-temple]')){if(b.textContent!=='Restore shared Collection Log')b.textContent='Restore shared Collection Log';b.title='Replace this browser’s Collection Log with the shared website snapshot. This can undo a newer local refresh or import; it does not fetch Temple.'}}

function enhanceDeveloper(){if(document.body.dataset.page!=='developer')return;const link=$('#developerRefreshLink');if(link)link.href='https://github.com/MarnixS/fun/actions/workflows/temple-cache.yml';const clog=$('#collection-log-refresh .btn');if(clog){clog.textContent='Update Collection Log via Temple';clog.title='Open the manual GitHub workflow that retrieves the latest Collection Log data from Temple.'}}
function observeDynamicUi(){let scheduled=false;const o=new MutationObserver(()=>{if(scheduled)return;scheduled=true;setTimeout(()=>{scheduled=false;simplifyMemberFilters();removeOwnerControls();hardenSavedClogReload()},0)});o.observe(document.documentElement,{childList:true,subtree:true})}
window.addEventListener('ug:page-rendered',renderOverviewClog);
window.addEventListener('ug:members-changed',renderOverviewClog);
window.addEventListener('ug:data-updated',renderOverviewClog);
async function init(){simplifyMemberFilters();removeOwnerControls();hardenSavedClogReload();enhanceDeveloper();observeDynamicUi();await renderOverviewClog();setTimeout(()=>{simplifyMemberFilters();removeOwnerControls();hardenSavedClogReload();renderOverviewClog()},1000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
